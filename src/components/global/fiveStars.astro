---
// svg Components
import { StarIcon } from "../../assets/icons";

---

<div class="stars">
  {Array.from({ length: 5 }, () => <StarIcon />)}
</div>

<style>
  .stars {
    color: var(--clr-gold);
    display: flex;
    gap: 0.5rem;
    justify-content: center;
  }
</style>
